# VPS 监控系统资源占用排查报告

## 执行日期
2026-01-12

## 发现的问题

### 🔴 严重问题

#### 1. 数据库无限增长
**位置**: `server.py` - `status_log` 表

**问题描述**:
- 数据库没有任何清理机制，每15分钟插入一条记录
- 假设10台VPS运行，每天产生：10 × 96 = 960条记录
- 一个月：28,800条记录
- 一年：350,000+条记录

**影响**:
- 硬盘空间持续增长
- 查询速度逐渐变慢
- 可能导致VPS存储空间耗尽

**已修复**: ✅
- 添加 `cleanup_old_data()` 函数
- 默认保留30天数据，可配置
- 每24小时自动清理一次
- 清理后执行 VACUUM 回收空间

---

#### 2. 客户端日志文件无限增长
**位置**: `client.py` - `monitor_client.log`

**问题描述**:
- 日志文件没有轮转机制
- 每15分钟写入至少2条日志
- 长期运行会导致日志文件越来越大

**影响**:
- 硬盘空间浪费
- 日志文件过大导致难以查看

**已修复**: ✅
- 使用 `RotatingFileHandler`
- 单个日志文件最大10MB
- 保留3个备份文件（总共40MB上限）

---

### 🟡 中等问题

#### 3. 图表数据查询可能过载
**位置**: `server.py` - `get_chart_data()`

**问题描述**:
- 原先限制1000条记录，但没有默认时间范围
- 如果数据量大，仍会加载大量数据到内存

**影响**:
- 服务器内存占用增加
- Web界面加载变慢

**已修复**: ✅
- 减少查询限制到500条
- 未指定日期时，默认只查询最近7天数据
- 优化查询逻辑

---

#### 4. 通知记录表无清理
**位置**: `server.py` - `alert_log` 表

**问题描述**:
- 每次发送通知都会记录
- 没有清理机制

**影响**:
- 数据库持续增长（影响较小）

**已修复**: ✅
- 在 `cleanup_old_data()` 中一并清理
- 与状态记录使用相同的保留策略

---

## 优化后的配置选项

在 `config.json` 中新增了两个配置项：

```json
{
    "data_retention_days": 30,        // 数据保留天数（默认30天）
    "cleanup_interval_hours": 24      // 清理间隔（默认24小时）
}
```

### 配置建议：

| VPS数量 | 建议保留天数 | 预估数据库大小 |
|---------|-------------|---------------|
| 1-5台   | 60天        | ~10-20MB     |
| 6-10台  | 30天        | ~15-30MB     |
| 11-20台 | 15天        | ~20-40MB     |
| 20台以上 | 7天         | ~15-30MB     |

---

## 资源占用预估

### 修复前（无清理机制）

**假设场景**: 10台VPS，运行6个月

- **数据库大小**: 约 150-200MB
- **客户端日志**: 每台约 50-100MB
- **服务端内存**: 随查询增加，可能达到 500MB+

### 修复后（30天保留策略）

**假设场景**: 10台VPS，持续运行

- **数据库大小**: 稳定在 20-30MB
- **客户端日志**: 每台最多 40MB
- **服务端内存**: 控制在 100-200MB

---

## 使用建议

### 1. 首次部署
建议的 `config.json` 配置：

```json
{
    "data_retention_days": 30,
    "cleanup_interval_hours": 24
}
```

### 2. 低存储空间VPS
如果VPS硬盘空间紧张（<10GB），建议：

```json
{
    "data_retention_days": 7,
    "cleanup_interval_hours": 12
}
```

### 3. 需要长期历史记录
如果需要保留更多历史数据，建议定期备份数据库：

```bash
# 每周备份一次
cp monitor.db monitor_backup_$(date +%Y%m%d).db

# 保留最近4周的备份
find . -name "monitor_backup_*.db" -mtime +28 -delete
```

---

## 监控建议

### 定期检查数据库大小
```bash
# Windows
dir monitor.db

# Linux
ls -lh monitor.db
```

### 检查日志文件
```bash
# Windows
dir monitor_client.log*

# Linux
ls -lh monitor_client.log*
```

### 预期大小范围
- 数据库: 10-50MB（取决于VPS数量和保留天数）
- 每台客户端日志: 最多40MB（3个备份 × 10MB + 当前文件）

---

## 总结

### 修复的关键改进：
1. ✅ **数据库自动清理** - 防止硬盘空间耗尽
2. ✅ **日志轮转** - 限制日志文件大小
3. ✅ **查询优化** - 减少内存占用
4. ✅ **可配置策略** - 灵活调整保留时间

### 现在可以安全地长期运行：
- 硬盘占用受控
- 内存使用稳定
- 性能不会随时间降低

### 注意事项：
- 如果需要更长的数据保留期，建议增加 `data_retention_days`
- 在资源受限的VPS上，可以缩短保留天数
- 定期监控数据库大小，确保符合预期
