# 资源优化更新说明

## 更新日期
2026-01-12

## 更新原因
排查并修复了可能导致VPS资源耗尽的问题：
- 数据库无限增长
- 日志文件无限增长
- 内存占用过高

---

## 主要改动

### 1. Server端优化 (`server.py`)

#### A. 新增数据清理功能

**新增函数**: `cleanup_old_data()`
```python
# Automatically clean up data older than configured retention period
# Default: 30 days
# Also performs VACUUM to reclaim disk space
```

**功能**:
- 自动删除超过保留期限的数据
- 清理 `status_log` 和 `alert_log` 表
- 执行 VACUUM 回收硬盘空间
- 每24小时自动运行一次（可配置）

#### B. 优化图表数据查询

**修改函数**: `get_chart_data()`

**改进**:
- 未指定日期范围时，默认只查询最近7天
- 查询限制从1000条降低到500条
- 减少内存占用
- 提高响应速度

#### C. 新增配置选项

```python
DATA_RETENTION_DAYS = _config.get("data_retention_days", 30)
CLEANUP_INTERVAL_HOURS = _config.get("cleanup_interval_hours", 24)
```

#### D. 增强后台检查线程

**修改函数**: `background_checker()`

**新增功能**:
- 除了检查断联状态，还会定期清理旧数据
- 记录上次清理时间
- 根据配置的间隔自动执行清理

---

### 2. Client端优化 (`client.py`)

#### 日志轮转机制

**改动**: 使用 `RotatingFileHandler` 替代普通文件日志

**配置**:
```python
RotatingFileHandler(
    'monitor_client.log',
    maxBytes=10*1024*1024,  # 10MB per file
    backupCount=3,          # Keep 3 backup files
    encoding='utf-8'
)
```

**效果**:
- 单个日志文件最大10MB
- 自动创建备份文件（.log.1, .log.2, .log.3）
- 总日志大小不超过40MB
- 旧日志自动删除

---

### 3. 配置文件更新 (`config.example.json`)

#### 新增配置项

```json
{
    "data_retention_days": 30,        // 数据保留天数
    "cleanup_interval_hours": 24      // 清理间隔（小时）
}
```

---

## 使用说明

### 如果你是新用户

直接使用，系统会自动：
1. 保留最近30天的数据
2. 每24小时清理一次旧数据
3. 客户端日志自动轮转，不超过40MB

### 如果你已在运行旧版本

#### 步骤1: 更新代码
```bash
git pull
```

#### 步骤2: 添加新配置
在你的 `config.json` 中添加：
```json
{
    "data_retention_days": 30,
    "cleanup_interval_hours": 24
}
```

#### 步骤3: 重启服务

**服务端**:
```bash
# 停止旧进程
# 重新启动
python server.py
```

**客户端**:
```bash
# 停止旧进程
# 重新启动
python client.py
```

#### 步骤4: （可选）手动清理现有数据

如果你的数据库已经很大，可以手动清理：
```bash
sqlite3 monitor.db "DELETE FROM status_log WHERE server_timestamp < datetime('now', '-30 days');"
sqlite3 monitor.db "DELETE FROM alert_log WHERE alert_time < datetime('now', '-30 days');"
sqlite3 monitor.db "VACUUM;"
```

---

## 资源占用对比

### 修复前（无清理机制）

假设10台VPS，运行6个月：
- 💾 数据库: ~150-200MB
- 📝 客户端日志: 每台 ~50-100MB
- 🧠 服务端内存: ~500MB+

### 修复后（30天保留）

假设10台VPS，持续运行：
- 💾 数据库: ~20-30MB（稳定）
- 📝 客户端日志: 每台最多40MB
- 🧠 服务端内存: ~100-200MB

---

## 配置建议

### 低存储VPS（<10GB）
```json
{
    "data_retention_days": 7,
    "cleanup_interval_hours": 12
}
```

### 标准VPS（10-40GB）
```json
{
    "data_retention_days": 30,
    "cleanup_interval_hours": 24
}
```

### 大存储VPS（>40GB）
```json
{
    "data_retention_days": 90,
    "cleanup_interval_hours": 24
}
```

---

## 监控与验证

### 检查数据库大小
```bash
# Windows PowerShell
Get-Item monitor.db | Select-Object Name, Length

# Linux
ls -lh monitor.db
```

### 检查清理日志
服务端会输出清理信息：
```
数据库清理完成: 删除了 2880 条状态记录和 15 条通知记录
```

### 验证日志轮转
检查客户端目录：
```bash
monitor_client.log
monitor_client.log.1
monitor_client.log.2
monitor_client.log.3
```

---

## 常见问题

### Q: 我需要保留更长的历史数据怎么办？

A: 有两个选择：
1. 增加 `data_retention_days`（会占用更多空间）
2. 定期备份数据库后再清理

### Q: 清理会丢失重要数据吗？

A: 只会删除超过保留期限的历史记录，不影响：
- 当前VPS列表
- 最新状态
- 最近N天的历史记录

### Q: 可以禁用自动清理吗？

A: 不建议。如果确实需要，可以设置很大的值：
```json
{
    "data_retention_days": 36500,  // 100年
}
```

### Q: 已经运行很久，数据库很大怎么办？

A: 参考"步骤4: 手动清理现有数据"，或者：
```bash
# 备份旧数据
cp monitor.db monitor_backup.db

# 手动清理
python
>>> import sqlite3
>>> conn = sqlite3.connect('monitor.db')
>>> cursor = conn.cursor()
>>> cursor.execute("DELETE FROM status_log WHERE server_timestamp < datetime('now', '-30 days')")
>>> cursor.execute("DELETE FROM alert_log WHERE alert_time < datetime('now', '-30 days')")
>>> conn.commit()
>>> conn.execute('VACUUM')
>>> conn.close()
```

---

## 技术细节

### 数据清理时机
1. 服务器启动时不会立即清理
2. 后台线程每60秒检查一次
3. 距离上次清理超过配置的间隔时才执行
4. 清理完成后会执行VACUUM回收空间

### 日志轮转机制
- 使用Python标准库的 `RotatingFileHandler`
- 当文件达到10MB时自动轮转
- 保留3个备份文件
- 最旧的备份会被自动删除

### 性能影响
- 清理操作在后台执行，不影响正常监控
- VACUUM操作会锁定数据库，但通常只需几秒
- 查询优化后，Web界面响应更快

---

## 向后兼容性

✅ 完全兼容旧版本
- 如果没有新配置项，使用默认值
- 已有数据不会丢失
- 可以无缝升级

---

## 贡献者
- 资源优化: 2026-01-12

## 反馈
如有问题或建议，请提交 Issue。
